<div class="row">
  <div class="col-sm-2"></div>
  <div class="col-sm-8 results">
    <%- if @search.file_found %>
      <%= render 'searches/purchase_form' %>
    <%- else %>
      <div class="row center">       
        Sorry, we couldn't get the statement for 
        <%= @search.ticker %> for <%= @search.year %>.
        <%- if @search.year >= 2015 %> The statements for
        <%= @search.year %> might not be out yet. You can try looking
        for  <%= @search.year - 1 %>, or try again later.
        <%- else %>
          Please try again later, or contact us, and we will try to help.
        <% end %> 
      </div>
    <%- end %>
    </br>
    <div class="row center">
      <%= link_to 'Get another Excel Financial Statement', "/download-excel-financial-statements" %>
    </div>
    </br>
    <div class="row center">  
      If you like our site, let people know
    </div>
    <div class="row center">
      <%= render 'searches/twittershare' %>
    </div>
    </div>
  </div>
</div>
<div class="col-sm-2"></div>
