<%= simple_form_for(@site_event) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :event %>
    <%= f.input :event_time %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
